import {Platform} from 'react-native';
export const IS_ANDROID = Platform.OS === 'android';
import {record, userType} from '../types';

const getColorScheme = (answer: boolean) => {};

export {getColorScheme};
